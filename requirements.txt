# Dependencies are defined in pyproject.toml (the single source of truth).
#
# Install the GYSTC MCP server + GUI dashboard (editable):
#     pip install -r requirements.txt
#
# Server only (no GUI):   pip install .
# Server + dev/test:      pip install -e .[dev]
-e .[dashboard]
